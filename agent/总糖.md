# 总糖需求.md 实现进度

状态：全部完成（4/4 条需求，含全部子项）

最后更新：2026-09-05

说明：按用户简化后的要求，导出 Excel 不复制原表单元格样式，仅保证内容完整（列布局、变量、公式与原表一致）。

## 需求 1：首页增加"总糖（正/反滴、蔗糖计）"功能入口，实现 糖.xlsx 四个总糖表功能 ✅

- [tools.ts](../src/data/tools.ts)：新增功能入口 id='total-sugar'，名称"总糖（正/反滴、蔗糖计）"，支持首页搜索（keywords：总糖/正滴/反滴/蔗糖计/滴定/糖含量）
- [router/index.ts](../src/router/index.ts)：注册路由 `/tools/total-sugar`，以及历史列表/明细路由新增 type=`total-sugar`
- [TotalSugarCalcView.vue](../src/views/TotalSugarCalcView.vue)：新建独立计算页（样品信息 + 总糖滴定面板 + 保存/重置），导航栏右上角进入历史记录
- [TotalSugarPanel.vue](../src/components/sugar/TotalSugarPanel.vue)：新建总糖滴定面板（正反滴切换、蔗糖计勾选、共享变量、平行样输入、公式与结果展示）
- 计算公式与实验室 Excel（糖.xlsx）四个总糖表完全一致（[sugarCalc.ts](../src/utils/sugarCalc.ts) `calcTotalSugar`）：
  - 总糖正滴：`ROUND(G量 × 100 × 稀释倍数 × 定容 × 100 / (50 × 称样量 × 滴定量 × 1000), 2)`，糖含量保留 2 位小数
  - 总糖正滴（蔗糖计）：`ROUND(G量 × 100 × 稀释倍数 × 100 × 定容 / (称样量 × 滴定量 × 50 × 1000) × 0.95, 2)`
  - 总糖反滴：`(G量 − 滴定量) × 定容 × 100 × 100 / (称样量 × 10 × 1000 × 50)`，不取整（与原表一致）
  - 总糖反滴（蔗糖计）：`(G量 − 滴定量) × 定容 × 100 × 0.95 × 100 / (50 × 称样量 × 10 × 1000)`，不取整
- 定容体积支持 250/200 两档（默认 250）：原表绝大多数公式定容为 250，少数为 200（正滴 24 行、正滴蔗糖计 2 行、反滴 8 行），界面提供"定250/定200"切换并贯穿计算、记录存储与导出公式
- 校验规则：标定G量 0~50mL（滴定管量程）、称样量 0.1~60g、滴定量 0~50mL；反滴模式滴定量校验上限动态跟随标定G量（滴定量 ≥ G量 时含量 ≤0，界面给出提示）

## 需求 2：正反滴切换 + "是否启用蔗糖计"勾选 ✅

- 正反滴切换：单一切换按钮位于"总糖滴定测定"标题行最右侧（胶囊按钮，文案随当前模式显示"切换反滴"/"切换正滴"），点击直接切换，无需确认；默认正滴模式
- 蔗糖计勾选：界面提供"是否启用蔗糖计"勾选框，启用后公式带 ×0.95 系数（对应原表带"（蔗糖计）"的两个表）；变量集与普通表相同（标定G量、稀释倍数（正滴）、定容体积、两平行样称样量/滴定量），仅计算公式不同
- 切换时共享变量区自动显隐：稀释倍数仅正滴模式显示；公式展示、精密度限值（正滴 ≤5%、反滴 ≤10%）随模式与蔗糖计状态联动

## 需求 3：平行样 2 自动生成、保存历史记录、历史记录查看（含批量删除与导出） ✅

### 平行样 2 自动生成

- 只需填写平行样 1（共享变量 + 称样量 + 滴定量），平行样 2 自动生成（只读展示），"重新生成"按钮可重新随机生成
- 生成规则（[sugarCalc.ts](../src/utils/sugarCalc.ts) `genTotalSugarRun2`）：
  - 称样质量在平行样 1 基础上 ±0.005g 内微调，保留 4 位小数，钳位 0.1~60g
  - 滴定量按 0.3%~5% 偏移，保留 2 位小数，钳位滴定管量程 0~50mL；反滴模式下滴定量必须小于标定G量
  - 两平行样糖含量误差满足精密度要求：正滴 <5%、反滴 <10%
  - 20 次尝试内不满足时兜底沿用平行样 1 数据
  - 历史记录编辑回填时通过 `suppressGen` 标志屏蔽自动生成，避免覆盖已有数据

### 保存计算结果到历史记录

- [history.ts](../src/utils/history.ts)：新增 `TotalSugarRecord`（含模式、蔗糖计标志、标定G量、稀释倍数、定容体积、两平行样数据与含量、平均值/误差/质量差），存储 key=`lab_history_total_sugar`；`addTotalSugarRecord`/`updateTotalSugarRecord` 增改记录

### 历史记录查看/编辑/批量删除/导出

- [HistoryView.vue](../src/views/HistoryView.vue)：总糖历史复用按日期分组、单选/多选/全选/按日期勾选、批量删除（带确认弹窗）、批量导出能力；记录卡片显示模式标签（如"正滴 · 蔗糖计 · 定200"）与含量₁/含量₂/平均值/相对误差（超精密度标红）
- [HistoryDetailView.vue](../src/views/HistoryDetailView.vue)：记录明细查看与编辑（TotalSugarPanel 回填，"保存修改"后按公式自动重算并更新记录）
- Excel 导出（[exportExcel.ts](../src/utils/exportExcel.ts) `exportTotalSugarHistory`）：
  - 按记录"模式 × 蔗糖计"分四个 sheet（仅有记录的表才生成）：总糖正滴 / 总糖反滴 / 总糖正滴（蔗糖计）/ 总糖反滴（蔗糖计），sheet 名与原表一致
  - 列布局与原表一致：正滴两表 B 编号 | C 名称 | D 标定G量 | E 称样量 | F 稀释倍数 | G 滴定量 | H 糖含量 | I 平均值 | J 误差 | K 定容备注 | L 质量差；反滴表 B 名称 | C 编号 | D 标定G量 | E 称样量 | F 滴定量 | G 糖含量 | H 平均值 | I 误差 | J 质量差；反滴（蔗糖计）表从 A 列起（A 名称 | B 编号 | C 标定G量 | D 称样量 | E 滴定量 | F 糖含量 | G 平均值 | H 误差 | I 定容备注）
  - 结果列写入公式（引用变量单元格，Excel 中修改变量自动重算）：正滴 `=ROUND(D*100*F*定容*100/50/E/G/1000,2)`（蔗糖计变体含 ×0.95）、平均值 `=ROUND((H+H')/2,1)`、误差 `=(H-H')*100/I`、质量差 `=H'-H`；反滴 `=(D-F)*定容*100*100/E/10/1000/50`（蔗糖计变体含 ×0.95）、平均值 `=(G+G')/2`、误差 `=ROUND((G-G')/H*100,1)`（蔗糖计不取整）、质量差 `=G-G'`
  - 同时写入缓存值保证打开即可见；文件名 `总糖正滴/总糖反滴/总糖正反滴_YYYYMMDD.xlsx`（含蔗糖计记录时追加"（蔗糖计）"）
  - 按用户简化要求不复制单元格样式（曾引入 xlsx-js-style 后按需移除，回退普通 xlsx），仅保证内容完整

## 需求 4：实现进度记录 ✅

- 即本文档

## 验证结果

- `npm run type-check`：通过（0 错误）；`npm run build`：构建成功；`npm run format`：已格式化
- Node 脚本数值验证（4027 项断言全部通过，验证后已清理临时文件）：
  - 与 糖.xlsx 原始数据逐组比对——总糖正滴 593 组平行样（跳过原表异常组 1 组）、总糖正滴（蔗糖计）70 组、总糖反滴 150 组、总糖反滴（蔗糖计）16 组，含量/平均值/误差/质量差全部一致
  - 平行样生成：1000 次全部满足基础约束（质量 ±0.005g、滴定量 2 位小数量程内、反滴滴定量 < 标定G量），1000 次全部满足精密度（正滴 <5%、反滴 <10%），0 失败
  - 导出冒烟测试：四种记录混合导出生成单个工作簿，四 sheet 名称、表头、两行一记录布局、结果列公式全部存在且回读一致

## 实现过程中发现并解决的问题

- JS 与 Excel 的 ROUND 舍入差异（如 92.44999999999999，JS Math.round 得 92.4、Excel 得 92.5）→ 实现 `excelRound`：先以 `toPrecision(15)` 消除浮点噪声，再按绝对值四舍五入，保证含量列与原表逐行一致
- 原表统计列取整规则四表各不相同：正滴（含蔗糖计）平均值 `ROUND(…,1)` 且误差除以取整后平均值、质量差 = 含量₂−含量₁（原表 L 列 H'−H）；反滴平均值不取整、误差 `ROUND(…,1)`、质量差 = 含量₁−含量₂（原表 J 列 G−G'）；反滴（蔗糖计）平均值/误差均不取整 → `totalSugarStats` 按表逐一区分实现
- 原表定容体积存在 250/200 两档（以公式内嵌常量为准），另有 2 行异常公式（正滴表 1052/1053 行：定容 100 且缺 /50 除数）→ 界面支持 250/200，异常组在数值验证中跳过
- 原表正滴表 K 列"定250"备注存在漂移笔误（定251~267、"."、空）→ 以 H 列公式实际定容体积为准确口径，导出统一写"定{定容体积}"
- 原表反滴（蔗糖计）表 I 列"定250"备注仅零散出现（r8/r11 等）→ 导出统一写入定容备注，保证内容完整
- 曾引入 xlsx-js-style 以复制单元格样式，用户简化需求（去掉"含单元格样式"）后移除该依赖，回退普通 xlsx，导出仅保证内容完整
