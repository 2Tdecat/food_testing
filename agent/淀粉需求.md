参考agent/requirement2_progress.md中的实现规则帮我完成下面的新需求：
1. 首页增加“淀粉（1/2法、正/反滴）”功能入口，该功能界面需要实现“agent/excels/糖.xlsx”中“淀粉一法”、“淀粉二法”、“淀粉1法反滴”、“淀粉2反滴”四个表的功能，这几个表名中“淀粉一”和“淀粉1”是一个意思，同理“淀粉2”和“淀粉二”也是同一个意思
2. "淀粉（1/2法、正/反滴）"界面同样需要实现切换正反滴功能，此外还需要增加一个“淀粉1法”和“淀粉2法”的切换按钮，按钮效果与切换正反滴按钮类似，点击即可切换，
3. 以下功能同样需要支持：自动生成平行样2的数据、保存计算结果到历史记录、历史记录查看页面（含批量删除和导出、导出的excel格式要与原excel格式一致，并且计算结果也要带公式，即在excel中修改变量也可以自动计算结果）
4. 实现完成后将实现进度记录到agent/淀粉需求.md中（本文档）

---

# 淀粉需求实现进度

状态：全部完成（4/4 条需求，含全部子项）

最后更新：2026-09-05

## 需求 1：首页增加"淀粉（1/2法、正/反滴）"功能入口，实现 糖.xlsx 四个淀粉表功能 ✅

- [tools.ts](../src/data/tools.ts)：新增功能入口 id='starch'，名称"淀粉（1/2法、正/反滴）"，支持首页搜索（keywords：淀粉/一法/二法/正滴/反滴/滴定/糖含量）
- [router/index.ts](../src/router/index.ts)：注册路由 `/tools/starch`，历史列表/明细路由 type 增加 `starch`
- [StarchCalcView.vue](../src/views/StarchCalcView.vue)：新建独立计算页（样品信息 + 淀粉面板 + 保存/重置），导航栏右上角进入历史记录
- [StarchPanel.vue](../src/components/sugar/StarchPanel.vue)：新建淀粉测定面板（正/反滴 + 1/2法双切换、共享变量、平行样输入、公式与结果展示）
- 计算公式与实验室 Excel（糖.xlsx）四个淀粉表完全一致（[sugarCalc.ts](../src/utils/sugarCalc.ts) `calcStarch`，0.9 为淀粉换算系数）：
  - 淀粉一法（正滴）：`ROUND(G量 × 100 × 稀释 × 定容 × 100 × 0.9 / (50 × 称样量 × 滴定量 × 1000), 2)`，糖含量保留 2 位小数
  - 淀粉二法（正滴）：`G量 × 100 × 稀释 × 定容 × 0.9 / (称样量 × 滴定量 × 1000)`，不取整（与原表一致）
  - 淀粉1法反滴：`(G量 − 滴定量) × 250 × 100 × 100 × 0.9 / (称样量 × 10 × 1000 × 50)`，定容固定 250，不取整
  - 淀粉2反滴：`(G量 − 滴定量) × 定容 × 100 × 稀释 × 0.9 / (称样量 × 10 × 1000)`，不取整
- 统计列取整规则与原表逐一致（`starchStats`）：
  - 淀粉一法：平均值 `ROUND(…,1)`；误差 = (含量₁−含量₂)×100/取整后平均值（不取整）
  - 淀粉二法 / 淀粉2反滴：平均值、误差均不取整
  - 淀粉1法反滴：平均值不取整；误差 `ROUND(…,1)`
- 数据分析发现原表存在定容体积公式变体：正滴两法 250mL 为主、少量 200mL（"定200"）；反滴2法 250/500mL（"定500"）；反滴1法固定 250 → 面板按模式/方法动态提供定容体积选项（正滴 250/200、反滴2法 250/500、反滴1法固定 250 不显示），存储 `flaskVolume` 字段并贯穿计算、导出公式与"定N"备注
- 校验规则：`RULE_TITR_G`（标定G量 0~50）、`RULE_TITR_MASS`（称样量 0.1~60g）、`RULE_DILUTION`（稀释倍数 ≥1）；反滴模式滴定量上限动态跟随标定G量（滴定量 ≥ G量 时含量 ≤0，界面提示）

## 需求 2：正/反滴切换 + 淀粉1法/2法切换 ✅

- [StarchPanel.vue](../src/components/sugar/StarchPanel.vue)：两个胶囊切换按钮均位于"淀粉滴定测定"标题行，点击直接切换，无需确认；默认正滴 + 淀粉1法
  - 正反滴按钮文案随模式显示"切换反滴"/"切换正滴"；1/2法按钮显示"切换2法"/"切换1法"
  - 两按钮组合出四种表形态：一法正滴 / 二法正滴 / 1法反滴 / 2法反滴，公式与精密度限值联动
- 共享变量区自动显隐：稀释倍数在正滴两法与反滴2法显示（反滴1法不使用）；定容体积在正滴（250/200）与反滴2法（250/500）显示，反滴1法定容固定 250 不显示；切换后定容不在可选项内时自动回退 250

## 需求 3：平行样 2 自动生成、保存历史记录、历史记录查看（含批量删除与导出） ✅

### 平行样 2 自动生成

- 只需填写平行样 1（共享变量 + 称样量 + 滴定量），平行样 2 自动生成（只读展示，"重新生成"按钮可重新随机生成）
- 生成规则（[sugarCalc.ts](../src/utils/sugarCalc.ts) `genStarchRun2`）：
  - 称样质量在平行样 1 基础上 ±0.005g 内微调，保留 4 位小数，钳位 0.1~60g
  - 滴定量按 0.3%~5% 偏移，保留 2 位小数，钳位滴定管量程 0~50mL；反滴模式下滴定量必须小于标定G量
  - 两平行样误差满足精密度要求：正滴 <5%、反滴 <10%（GB 5009.7-2016 沿用滴定法限值）
  - 20 次尝试内不满足时兜底沿用平行样 1 数据；历史编辑回填时 `suppressGen` 屏蔽自动生成

### 保存计算结果到历史记录

- [history.ts](../src/utils/history.ts)：新增 `StarchRecord`（模式、方法、标定G量、稀释倍数、定容体积、两平行样数据与含量、平均值/误差），存储 key=`lab_history_starch`；`addStarchRecord`/`updateStarchRecord` 增改记录

### 历史记录查看/编辑/批量删除/导出

- [HistoryView.vue](../src/views/HistoryView.vue)：淀粉历史复用按日期分组、单选/多选/全选、批量删除（确认弹窗）、批量导出；卡片显示模式/方法标签（如"正滴·一法·定250"）与含量₁/含量₂/平均值/误差（超精密度标红）
- [HistoryDetailView.vue](../src/views/HistoryDetailView.vue)：记录明细查看与编辑（StarchPanel 回填，保存后自动重算更新）
- Excel 导出（[exportExcel.ts](../src/utils/exportExcel.ts) `exportStarchHistory`）：
  - 按 模式×方法 分四个 sheet：淀粉一法 / 淀粉二法 / 淀粉1法反滴 / 淀粉2反滴（仅实际存在记录的 sheet 才生成）
  - 列布局与原表逐列一致：一法 A 编号 | B 名称 | C 标定G量 | D 称样量 | E 稀释 | F 滴定量 | G 糖含量 | H 平均值 | I 误差 | J 质量差（含"定N"）；二法/2反滴 B/C 编号名称（照抄原表 B"样品名称"/C"样品编号"表头互换笔误）、二法第 2 行为空行、数据自第 3 行起；1法反滴 A"样品名称"/B"样品编号"同样照抄原表互换笔误
  - 结果列写入公式（修改变量自动重算）并写缓存值：一法 `=ROUND(C*100*E*定容*100*0.9/50/D/F/1000,2)`、`ROUND((G+G')/2,1)`、`=(G-G')*100/H`；二法 `=D*100*F*定容*0.9/E/G/1000`、`=(H+H')/2`、`=(H-H')/I*100`；1法反滴 `=(C-E)*250*100*100*0.9/D/10/1000/50`、`=(F+F')/2`、`ROUND((F-F')/G*100,1)`；2反滴 `=(D-G)*定容*100*F*0.9/E/10/1000`、`=(H+H')/2`、`=(H-H')/I*100`
  - 文件名：`淀粉正滴1法/淀粉正滴2法/淀粉反滴1法/淀粉反滴2法/淀粉正反滴_YYYYMMDD.xlsx`（按记录的模式×方法组合命名）

## 需求 4：实现进度记录 ✅

- 即本文档

## 验证结果

- `npm run type-check`：通过（0 错误）；`npm run build`：构建成功；`npm run format`：已格式化
- Node 脚本数值验证（tmp_starch/validate_starch.cjs，直接调用 tsc 编译后的真实源码，27693 项断言全部通过，验证后已清理临时目录）：
  - 计算基准：与 糖.xlsx 逐行比对——淀粉一法 114 对平行样（含量断言 224，最大偏差 0）、淀粉二法 460 对（含量断言 916，最大偏差 5.0e-14）、淀粉1法反滴 46 对（含量断言 92，最大偏差 4.8e-14）、淀粉2反滴 68 对（含量断言 136），统计断言共 1162（平均值/误差取整规则一致）
  - 平行样生成：7 种模式×方法×定容组合（正滴 250/200、反滴 250/500）2460 次常规生成全部满足质量 ±0.005g、滴定量 2 位小数量程内、正滴误差 <5%（最差 <5.0%）、反滴滴定量 < 标定G量且误差 <10%（最差 <10.0%）；边界场景（称样量贴近 0.1/60g、滴定量贴近 50mL 量程、反滴贴近 G量）1240 次均不越界
  - Excel 导出：工作表名、表头、空行结构、编号/名称列位（照抄原表表头互换笔误）、"定N"备注、公式字符串（含定200/定500 变体公式）全部与预期一致；公式语义独立复算与 TS 计算/缓存值一致

## 实现过程中发现并解决的问题

- 原表定容体积变体：正滴两法存在"定200"（200mL）、反滴2法存在"定500"（500mL）公式变体 → 定容体积参数贯穿计算、界面选择、记录存储、导出公式与"定N"备注
- 原表平行对共享变量不一致的异常行（本应用按标准做法共享变量，统计不比对）：
  - 淀粉一法 r203/204、淀粉二法 r27/28、r112/113、r232/233：平行对两行稀释倍数不同（1≠2、2≠4、2≠1），原表含量公式按本行变量计算（本应用含量复算与原表逐行一致），但平均值/误差无法用单一共享稀释复现
  - 淀粉2反滴 r107-112：平行对两行标定G量不同（11/12、13/14、15/16），同上
- 原表过期缓存/手工覆盖行：
  - 淀粉一法 r203：滴定量缺失（F204 空），缓存平均值/误差（7/7）与其自身公式按含量缓存复算（25.2/144.6）不符（原表手动计算模式未重算）
  - 淀粉二法 r864-867：含量为手工录入值（如 49.31 vs 公式复算 48.6718、65.183 vs 76.2533），与输入数据不对应；其平均值/误差公式按手工值计算正确（本应用 starchStats 用手工含量复算与原表缓存完全一致，验证了取整规则）
  - 淀粉二法 r251/252：输入整体被删、仅剩结果值；用缓存含量验证统计一致
- 原表二法/2反滴/1法反滴表头 B"样品名称"/C"样品编号"（或 A/B）与实际内容互换 → 导出照抄原表表头与列位，保证与原表格式一致
- 原表淀粉二法第 2 行为空行、数据自第 3 行起（其余三表数据自第 2 行起）→ 导出逐 sheet 对齐行位置
- 验证脚本小数位断言最初用浮点严格相等（如 1.4142×10000 = 14141.999…）产生误报 → 改为容差判断（1e-6）