需求变更：
1. 红糖与还原糖功能页面：删除第二个滴定体积（vβ），需要同步调整导出 Excel 的格式，删除对应列
2. “红糖与还原糖”和“绵白糖蔗糖分-5012”界面：
   a. 只需要填写平行样 1 ，平行样2的数据自动根据平行样 1 的数据生成。
   b. 要求自动生成的平行样 2 的数据要符合：平行样 2 的数据计算出来的结果与平行样1的结果相差小于0.05%（蔗糖结果误差小于0.05%，还原糖小于15%）
   c. 提供“重新生成”按钮，点击可重新生成平行样 2 的数据
3. 首页功能名称调整，“绵白糖蔗糖分-5012”改为“绵白糖红糖蔗糖分”
4. 两个功能的历史记录页面：
   a. 需要支持按日期分类，分类名称为年-月-日，默认展开今天的分类
   b. 需要支持批量编辑（目前的批量操作有：删除、导出），支持单选、多选、全选、全不选、按日期勾选（勾选/取消勾选日期分类时自动勾选/取消勾选分类下的数据）
   c. Excel导出时结果列需要带计算公式，需要实现在Excel 中修改变量时结果依然会自动计算（与原 Excel 的效果一致）
   d. 目前删除按钮点不到，点击删除按钮也会跳转到详情页面，请修复，修复时请确认删除操作必须要有确认操作，删除前必须弹窗询问用户是否确定删除。